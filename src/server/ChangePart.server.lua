local myPart = game.Workspace.Part -- gets the part
myPart.Color = Color3.new(1, 0, 0) -- makes it red
myPart.Transparency = 0.3 -- slightly see-through
myPart.Anchored = true -- locks it in place
print("Part updated!")
